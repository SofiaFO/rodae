const express = require('express');
const router = express.Router();

// Placeholder para rotas de corrida
router.get('/', (req, res) => {
  res.json({ message: 'Rotas de corrida em desenvolvimento' });
});

module.exports = router;
